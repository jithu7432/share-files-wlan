from setuptools import setup

setup(
    name="host",
    version="1.0",
    description="share files over wlan",
    author="Jithin Johnson",
    author_email="",
    packages=["host"],
    install_requires=["pyqrcode"],
)
